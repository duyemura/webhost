// Browser-safe stub for src/config.ts — used when backend renderers run in the Vite bundle.
// Only the fields actually referenced by block renderers need to be present.
export const config = { googleMapsApiKey: "" } as const;
